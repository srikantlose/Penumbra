# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Dependencies
node_modules/
dist/
build/
target/
.next/
*.tsbuildinfo

# Environment & config
.env
.env.local
*.local.json
.claude/

# Local docs (untracked context)
CLAUDE.md

# Temporary
*.tmp
.turbo/
.pnpm-store/

# Playwright output (apps/web)
test-results/
playwright-report/
blob-report/

# Vendored third-party MCP servers (not part of this project; rebuilt locally, not committed)
tools/

# Session-scoped agent state (machine-local, regenerated freely)
.stitch-project.json
HANDOFF.md

# Syzygy tablebases (~1 GB, fetched on demand via scripts/fetch-syzygy.mjs)
tablebases/

# Engine binaries + networks (~1 GB, fetched on demand via scripts/fetch-engines.mjs)
# Root-anchored: services/analysis/src/engines/ is real source, not this.
/engines/

# Calibration corpus (built via services/analysis's build-calibration-corpus
# script; frozen positions dataset, not source -- see docs/FOG_INDEX_METHODOLOGY.md)
/corpus/

# DB + corpus backups (scripts/backup-calibration.ps1), machine-local
/backups/
