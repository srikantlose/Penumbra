# Copy to .env.local (gitignored) and fill in real values. Never commit actual keys.

# Google Stitch API key — stitch.withgoogle.com -> profile menu -> API key -> Create key.
# Required for the "stitch" MCP server (see .mcp.json) to authenticate.
STITCH_API_KEY=
